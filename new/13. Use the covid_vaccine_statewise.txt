13. Use the covid_vaccine_statewise.csv dataset and perform the following
 analytics.
 a. Describe the dataset
 b. Number of persons state wise vaccinated for first dose in India
 c. Number of persons state wise vaccinated for second dose in India


code:

import pandas as pd

df = pd.read_csv(r"E:\TY\DSML_dataset\Covid Vaccine Statewise.csv")

first_dose = df.groupby("State")["First Dose Administered"].sum().sort_values(ascending=False)

first_dose

second_dose= df.groupby("State")["Second Dose Administered"].sum().sort_values(ascending=False)

second_dose